package com.financeapp.enums;

/**
 * User roles for authorization.
 * ROLE_USER  — standard user, can manage their own finances
 * ROLE_ADMIN — administrator, can view all users and platform stats
 */
public enum Role {
    ROLE_USER,
    ROLE_ADMIN
}
